//this is a scratch paper for programmers 

let cpu = ["rock" , "paper" , "scisorrs", "lizzard", "spock"];

function randChoice(X){
    return Math.floor(Math.random()*X)
}

let cpuChoice = randChoice(cpu.lenght) 


console.log(cpu[randNum]);
console.log(cpu[playersChoice]);